#include "MainHeader.h"

#include<cstdio>
#include<iostream>

using namespace std;
/*
void JULKA()
{
	short test;
	scanf("%hd",&test);

	for(;test>0;test--)
	{
		short total[110],excess[110],result[110];
		int totalLength,excessLength,resultLength;
		char c;
		int i;
		for(i=0;(c=getchar())!='\n' && i<110;i++)
		{
			total[i] = c-'0';
		}
		totalLength = i;
		for(i=0;(c=getchar())!='\n' && i<110;i++)
		{
			excess[i] = c-'0';
		}
		excessLength = i;

		// Klaudia has excess+total / 2
		// natalia has total - excess / 2
		add(total,totalLength,excess,excessLength,result,&resultLength);



	}
}
*/
